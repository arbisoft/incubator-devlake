{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": {
          "type": "datasource",
          "uid": "grafana"
        },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "panels": [
    {
      "datasource": null,
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "options": {
        "code": {
          "language": "plaintext",
          "showLineNumbers": false,
          "showMiniMap": false
        },
        "content": "**How to read this dashboard**\n\n- **Escaped Defect Rate** only counts issues classified as `BUG`/`INCIDENT` in the connected trackers. In practice that means Jira \u2014 GitHub and GitLab issues here don't carry a type classification yet, so a GitHub/GitLab-only project will show a rate near zero, which reflects missing classification rather than an absence of defects. Check the \"Defect-Rate Coverage by Project\" table below before reading this metric for a given project.\n- **Contributors** is deduplicated by `accounts.full_name` (falling back to the PR author name when no account matches), because one engineer can accumulate several `accounts` rows as their git client runs from different machines and IPs. This narrows the overcount but doesn't fully collapse it \u2014 see the \"Distinct-Account Sanity Check\" table below for the residual gap.\n- **AI Spend and Cost per Merged PR are company-wide by design.** AI activity carries no project or repo association in the schema, so scoping the PR side to a single project while leaving spend company-wide would inflate the ratio. Both sides stay pinned to all projects so the number means the same thing no matter what the Project filter above is set to.\n- AI Spend combines two sources: legacy Cursor / Claude-platform activity records cost directly, while Claude Code (OTel) records cost on a separate usage table. Both are combined into one company-wide total.",
        "mode": "markdown"
      },
      "pluginVersion": "11.0.0",
      "title": "About this dashboard",
      "type": "text",
      "gridPos": {
        "h": 8,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "id": 1
    },
    {
      "datasource": "mysql",
      "description": "Total estimated AI spend across all connected AI tools and providers, company-wide. Combines legacy Cursor/Claude-platform cost with Claude Code (OTel) usage cost into one daily total.",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          },
          "unit": "currencyUSD"
        },
        "overrides": []
      },
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        },
        "textMode": "auto"
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH ai_spend_daily AS (\n  SELECT date, provider, user_email, account_id, estimated_cost_usd AS cost\n  FROM ai_activities WHERE record_kind IS NULL\n  UNION ALL\n  SELECT date, provider, user_email, account_id, estimated_cost_usd AS cost\n  FROM ai_model_usages\n)\nSELECT SUM(cost) AS \"AI Spend\" FROM ai_spend_daily WHERE $__timeFilter(date)",
          "refId": "A"
        }
      ],
      "title": "AI Spend (Company-wide)",
      "type": "stat",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 0,
        "y": 8
      },
      "id": 2
    },
    {
      "datasource": "mysql",
      "description": "Merged pull requests, scoped to the selected project(s).",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "blue",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        },
        "textMode": "auto"
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT COUNT(*) AS \"PRs Merged\"\nFROM project_pr_metrics ppm\nJOIN pull_requests pr ON pr.id = ppm.id\nWHERE ppm.project_name IN ($project) AND $__timeFilter(pr.merged_date)",
          "refId": "A"
        }
      ],
      "title": "PRs Merged",
      "type": "stat",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 6,
        "y": 8
      },
      "id": 3
    },
    {
      "datasource": "mysql",
      "description": "Counts issues where `type IN ('TASK','SUBTASK')` and `resolution_date IS NOT NULL`. resolution_date is used as the \u201cdone\u201d signal instead of matching status strings, because status values aren't normalized across connected trackers \u2014 Jira gives DONE/TODO/IN_PROGRESS, while other sources leak in stray values like Closed/New/Ready for test/In progress. resolution_date correlates closely with a DONE-equivalent status and is rarely set on non-terminal issues, making it a more reliable completion signal across sources.",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "purple",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        },
        "textMode": "auto"
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT COUNT(*) AS \"Tasks Done\"\nFROM issues i\nJOIN board_issues bi ON bi.issue_id = i.id\nJOIN project_mapping pm ON pm.row_id = bi.board_id AND pm.`table` = 'boards'\nWHERE pm.project_name IN ($project)\n  AND i.type IN ('TASK','SUBTASK')\n  AND i.resolution_date IS NOT NULL\n  AND $__timeFilter(i.resolution_date)",
          "refId": "A"
        }
      ],
      "title": "Tasks Done",
      "type": "stat",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 12,
        "y": 8
      },
      "id": 4
    },
    {
      "datasource": "mysql",
      "description": "Distinct pull request authors, scoped to the selected project(s) and deduplicated by `accounts.full_name` (falling back to the PR author name when no account matches). This narrows but doesn't fully eliminate the overcount from fragmented accounts \u2014 see the Distinct-Account Sanity Check table below.",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "orange",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        },
        "textMode": "auto"
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT COUNT(DISTINCT COALESCE(NULLIF(a.full_name,''), pr.author_name)) AS \"Contributors\"\nFROM project_pr_metrics ppm\nJOIN pull_requests pr ON pr.id = ppm.id\nLEFT JOIN accounts a ON a.id = pr.author_id\nWHERE ppm.project_name IN ($project) AND $__timeFilter(pr.merged_date)",
          "refId": "A"
        }
      ],
      "title": "Contributors",
      "type": "stat",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 18,
        "y": 8
      },
      "id": 5
    },
    {
      "datasource": "mysql",
      "description": "PRs Merged \u00f7 Contributors, both scoped to the selected project(s).",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "blue",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        },
        "textMode": "auto"
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT ROUND(prs.n / NULLIF(c.n,0), 1) AS \"PRs per Contributor\"\nFROM\n  (SELECT COUNT(*) n FROM project_pr_metrics ppm JOIN pull_requests pr ON pr.id=ppm.id WHERE ppm.project_name IN ($project) AND $__timeFilter(pr.merged_date)) prs,\n  (SELECT COUNT(DISTINCT COALESCE(NULLIF(a.full_name,''), pr.author_name)) n FROM project_pr_metrics ppm JOIN pull_requests pr ON pr.id=ppm.id LEFT JOIN accounts a ON a.id=pr.author_id WHERE ppm.project_name IN ($project) AND $__timeFilter(pr.merged_date)) c",
          "refId": "A"
        }
      ],
      "title": "PRs per Contributor",
      "type": "stat",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 0,
        "y": 12
      },
      "id": 6
    },
    {
      "datasource": "mysql",
      "description": "Tasks Done \u00f7 Contributors. The numerator is scoped by the issue tracker's board, the denominator by PR authorship \u2014 the two contributor pools aren't identical, so this is a directional ratio rather than an exact one.",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "purple",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        },
        "textMode": "auto"
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT ROUND(t.n / NULLIF(c.n,0), 1) AS \"Tasks per Contributor\"\nFROM\n  (SELECT COUNT(*) n FROM issues i JOIN board_issues bi ON bi.issue_id=i.id JOIN project_mapping pm ON pm.row_id=bi.board_id AND pm.`table`='boards' WHERE pm.project_name IN ($project) AND i.type IN ('TASK','SUBTASK') AND i.resolution_date IS NOT NULL AND $__timeFilter(i.resolution_date)) t,\n  (SELECT COUNT(DISTINCT COALESCE(NULLIF(a.full_name,''), pr.author_name)) n FROM project_pr_metrics ppm JOIN pull_requests pr ON pr.id=ppm.id LEFT JOIN accounts a ON a.id=pr.author_id WHERE ppm.project_name IN ($project) AND $__timeFilter(pr.merged_date)) c",
          "refId": "A"
        }
      ],
      "title": "Tasks per Contributor",
      "type": "stat",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 6,
        "y": 12
      },
      "id": 7
    },
    {
      "datasource": "mysql",
      "description": "(BUG/INCIDENT issues opened within 14 days of a production deployment) \u00f7 (PRs merged in the same window) \u00d7 100, scoped to the selected project(s). A deployment counts as production when its environment field contains \"prod\" (matched case-insensitively, since environment names vary by project \u2014 production, Prod, hks-prod, etc.). See the \"Defect-Rate Coverage by Project\" table below for which projects currently have defect-type data to compute this from.",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "red",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        },
        "textMode": "auto"
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH prod_deploys AS (\n  SELECT pm.project_name, cdc.finished_date\n  FROM cicd_deployment_commits cdc\n  JOIN project_mapping pm ON pm.row_id = cdc.cicd_scope_id AND pm.`table` = 'cicd_scopes'\n  WHERE pm.project_name IN ($project) AND LOWER(cdc.environment) LIKE '%prod%' AND cdc.result = 'SUCCESS' AND $__timeFilter(cdc.finished_date)\n),\ndefects AS (\n  SELECT DISTINCT i.id\n  FROM issues i\n  JOIN board_issues bi ON bi.issue_id = i.id\n  JOIN project_mapping pm ON pm.row_id = bi.board_id AND pm.`table` = 'boards'\n  JOIN prod_deploys pd ON pd.project_name = pm.project_name\n    AND i.created_date BETWEEN pd.finished_date AND DATE_ADD(pd.finished_date, INTERVAL 14 DAY)\n  WHERE pm.project_name IN ($project) AND i.type IN ('BUG','INCIDENT')\n),\nprs AS (\n  SELECT pr.id FROM project_pr_metrics ppm JOIN pull_requests pr ON pr.id = ppm.id\n  WHERE ppm.project_name IN ($project) AND $__timeFilter(pr.merged_date)\n)\nSELECT ROUND((SELECT COUNT(*) FROM defects) * 100.0 / NULLIF((SELECT COUNT(*) FROM prs), 0), 2) AS \"Escaped Defect Rate (per 100 PRs)\"",
          "refId": "A"
        }
      ],
      "title": "Escaped Defect Rate",
      "type": "stat",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 12,
        "y": 12
      },
      "id": 8
    },
    {
      "datasource": "mysql",
      "description": "AI Spend \u00f7 PRs Merged, both company-wide. Kept unscoped on both sides deliberately \u2014 since AI Spend has no project association, scoping only the PR side would shrink the denominator and inflate the ratio whenever a single project is selected.",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          },
          "unit": "currencyUSD"
        },
        "overrides": []
      },
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        },
        "textMode": "auto"
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH ai_spend_daily AS (\n  SELECT date, provider, user_email, account_id, estimated_cost_usd AS cost\n  FROM ai_activities WHERE record_kind IS NULL\n  UNION ALL\n  SELECT date, provider, user_email, account_id, estimated_cost_usd AS cost\n  FROM ai_model_usages\n)\nSELECT ROUND(spend.cost / NULLIF(prs.n, 0), 2) AS \"Cost per Merged PR\"\nFROM\n  (SELECT SUM(cost) AS cost FROM ai_spend_daily WHERE $__timeFilter(date)) spend,\n  (SELECT COUNT(*) n FROM pull_requests pr WHERE pr.status = 'MERGED' AND $__timeFilter(pr.merged_date)) prs",
          "refId": "A"
        }
      ],
      "title": "Cost per Merged PR (Company-wide)",
      "type": "stat",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 18,
        "y": 12
      },
      "id": 9
    },
    {
      "collapsed": false,
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 16
      },
      "id": 10,
      "panels": [],
      "title": "Indexed Trend (baseline = first month in range = 100)",
      "type": "row"
    },
    {
      "datasource": "mysql",
      "description": "Each metric indexed to its own first non-zero month in range (baseline = 100), so all four can be compared on the same trend scale even though they're sourced from different tables that don't necessarily start in the same month. AI Cost is company-wide; PRs Merged, Tasks Done, and Contributors are scoped to the selected project(s).",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "palette-classic"
          },
          "custom": {
            "axisBorderShow": false,
            "axisCenteredZero": false,
            "axisColorMode": "text",
            "axisLabel": "",
            "axisPlacement": "auto",
            "fillOpacity": 80,
            "gradientMode": "none",
            "hideFrom": {
              "legend": false,
              "tooltip": false,
              "viz": false
            },
            "lineWidth": 1,
            "scaleDistribution": {
              "type": "linear"
            },
            "thresholdsStyle": {
              "mode": "off"
            }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "AI cost"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "fixedColor": "#eb6834",
                  "mode": "fixed"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "PRs merged"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "fixedColor": "#1baf7a",
                  "mode": "fixed"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Tasks done"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "fixedColor": "#2a78d6",
                  "mode": "fixed"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Contributors"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "fixedColor": "#888780",
                  "mode": "fixed"
                }
              }
            ]
          }
        ]
      },
      "options": {
        "barRadius": 0,
        "barWidth": 0.7,
        "fullHighlight": false,
        "groupWidth": 0.7,
        "legend": {
          "calcs": [],
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "orientation": "auto",
        "showValue": "auto",
        "stacking": "none",
        "tooltip": {
          "mode": "multi",
          "sort": "none"
        },
        "xField": "month",
        "xTickLabelRotation": 0,
        "xTickLabelSpacing": 0
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH ai_spend_daily AS (\n  SELECT date, provider, user_email, account_id, estimated_cost_usd AS cost\n  FROM ai_activities WHERE record_kind IS NULL\n  UNION ALL\n  SELECT date, provider, user_email, account_id, estimated_cost_usd AS cost\n  FROM ai_model_usages\n),\nmonthly_cost AS (SELECT DATE_FORMAT(date, '%Y-%m-01') AS month, SUM(cost) AS v FROM ai_spend_daily WHERE $__timeFilter(date) GROUP BY 1),\nmonthly_prs AS (\n  SELECT DATE_FORMAT(pr.merged_date, '%Y-%m-01') AS month, COUNT(*) AS v\n  FROM project_pr_metrics ppm JOIN pull_requests pr ON pr.id = ppm.id\n  WHERE ppm.project_name IN ($project) AND $__timeFilter(pr.merged_date) GROUP BY 1\n),\nmonthly_tasks AS (\n  SELECT DATE_FORMAT(i.resolution_date, '%Y-%m-01') AS month, COUNT(*) AS v\n  FROM issues i JOIN board_issues bi ON bi.issue_id = i.id\n  JOIN project_mapping pm ON pm.row_id = bi.board_id AND pm.`table` = 'boards'\n  WHERE pm.project_name IN ($project) AND i.type IN ('TASK','SUBTASK') AND i.resolution_date IS NOT NULL AND $__timeFilter(i.resolution_date)\n  GROUP BY 1\n),\nmonthly_contrib AS (\n  SELECT DATE_FORMAT(pr.merged_date, '%Y-%m-01') AS month, COUNT(DISTINCT COALESCE(NULLIF(a.full_name,''), pr.author_name)) AS v\n  FROM project_pr_metrics ppm JOIN pull_requests pr ON pr.id = ppm.id LEFT JOIN accounts a ON a.id = pr.author_id\n  WHERE ppm.project_name IN ($project) AND $__timeFilter(pr.merged_date) GROUP BY 1\n),\nmonths AS (\n  SELECT month FROM monthly_cost UNION SELECT month FROM monthly_prs\n  UNION SELECT month FROM monthly_tasks UNION SELECT month FROM monthly_contrib\n),\nfilled AS (\n  SELECT m.month,\n    COALESCE(c.v, 0) AS cost, COALESCE(p.v, 0) AS prs, COALESCE(t.v, 0) AS tasks, COALESCE(ct.v, 0) AS contrib\n  FROM months m\n  LEFT JOIN monthly_cost c ON c.month = m.month\n  LEFT JOIN monthly_prs p ON p.month = m.month\n  LEFT JOIN monthly_tasks t ON t.month = m.month\n  LEFT JOIN monthly_contrib ct ON ct.month = m.month\n)\nSELECT month,\n  ROUND(cost * 100.0 / NULLIF((SELECT cost FROM filled f WHERE f.cost > 0 ORDER BY f.month LIMIT 1), 0), 1) AS `AI cost`,\n  ROUND(prs * 100.0 / NULLIF((SELECT prs FROM filled f WHERE f.prs > 0 ORDER BY f.month LIMIT 1), 0), 1) AS `PRs merged`,\n  ROUND(tasks * 100.0 / NULLIF((SELECT tasks FROM filled f WHERE f.tasks > 0 ORDER BY f.month LIMIT 1), 0), 1) AS `Tasks done`,\n  ROUND(contrib * 100.0 / NULLIF((SELECT contrib FROM filled f WHERE f.contrib > 0 ORDER BY f.month LIMIT 1), 0), 1) AS `Contributors`\nFROM filled\nORDER BY month",
          "refId": "A"
        }
      ],
      "title": "AI Cost (Company-wide) / PRs Merged / Tasks Done / Contributors \u2014 Indexed",
      "type": "barchart",
      "gridPos": {
        "h": 9,
        "w": 24,
        "x": 0,
        "y": 17
      },
      "id": 11
    },
    {
      "datasource": "mysql",
      "description": "Indexes the monthly count of escaped defects (BUG/INCIDENT issues matched to a production deployment within 14 days \u2014 see the Escaped Defect Rate KPI above) to its own first non-zero month. Shows trend shape only: it tracks the raw monthly defect count, not the precise per-100-PR rate shown in the KPI card.",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "palette-classic"
          },
          "custom": {
            "drawStyle": "line",
            "fillOpacity": 10,
            "lineWidth": 2,
            "pointSize": 5,
            "showPoints": "never",
            "spanNulls": false
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Escaped defects (count, unindexed ratio)"
            },
            "properties": [
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineStyle",
                "value": {
                  "fill": "dash",
                  "dash": [
                    10,
                    10
                  ]
                }
              },
              {
                "id": "custom.fillOpacity",
                "value": 0
              },
              {
                "id": "custom.lineWidth",
                "value": 2
              },
              {
                "id": "custom.pointSize",
                "value": 5
              },
              {
                "id": "custom.showPoints",
                "value": "always"
              },
              {
                "id": "custom.stacking",
                "value": {
                  "group": "line_overlay",
                  "mode": "none"
                }
              },
              {
                "id": "color",
                "value": {
                  "fixedColor": "#e34948",
                  "mode": "fixed"
                }
              }
            ]
          }
        ]
      },
      "options": {
        "legend": {
          "calcs": [],
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "tooltip": {
          "mode": "multi",
          "sort": "none"
        }
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH monthly AS (\n  SELECT DATE_FORMAT(i.created_date, '%Y-%m-01') AS month, 'Escaped defects (count, unindexed ratio)' AS metric, COUNT(DISTINCT i.id) AS value\n  FROM issues i JOIN board_issues bi ON bi.issue_id = i.id\n  JOIN project_mapping pm ON pm.row_id = bi.board_id AND pm.`table` = 'boards'\n  WHERE pm.project_name IN ($project) AND i.type IN ('BUG','INCIDENT') AND $__timeFilter(i.created_date)\n  GROUP BY 1\n)\nSELECT STR_TO_DATE(month, '%Y-%m-%d') AS time, metric,\n  ROUND(value * 100.0 / FIRST_VALUE(value) OVER (ORDER BY month), 1) AS value\nFROM monthly\nORDER BY time",
          "refId": "A"
        }
      ],
      "title": "Escaped Defects \u2014 Indexed (trend shape only)",
      "transformations": [
        {
          "id": "prepareTimeSeries",
          "options": {
            "format": "many"
          }
        },
        {
          "id": "renameByRegex",
          "options": {
            "regex": "^value (.+)$",
            "renamePattern": "$1"
          }
        }
      ],
      "type": "timeseries",
      "gridPos": {
        "h": 5,
        "w": 24,
        "x": 0,
        "y": 26
      },
      "id": 12
    },
    {
      "collapsed": false,
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 31
      },
      "id": 13,
      "panels": [],
      "title": "Data Quality",
      "type": "row"
    },
    {
      "datasource": "mysql",
      "description": "Compares the raw distinct `accounts.id` count for PR authors against the name-deduplicated count used for the Contributors KPI above, scoped to the selected project(s). The gap between the two is mostly caused by engineers accumulating multiple account rows when their git client reports different emails or hostnames across machines, rather than by genuinely distinct people.",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT\n  COUNT(DISTINCT pr.author_id) AS \"Raw Account Count\",\n  COUNT(DISTINCT COALESCE(NULLIF(a.full_name,''), pr.author_name)) AS \"Name-Deduped Count\",\n  ROUND(100.0 * (COUNT(DISTINCT pr.author_id) - COUNT(DISTINCT COALESCE(NULLIF(a.full_name,''), pr.author_name))) / NULLIF(COUNT(DISTINCT pr.author_id), 0), 1) AS \"Overcount %\"\nFROM project_pr_metrics ppm\nJOIN pull_requests pr ON pr.id = ppm.id\nLEFT JOIN accounts a ON a.id = pr.author_id\nWHERE ppm.project_name IN ($project) AND $__timeFilter(pr.merged_date)",
          "refId": "A"
        }
      ],
      "title": "Distinct-Account Sanity Check",
      "type": "table",
      "gridPos": {
        "h": 6,
        "w": 12,
        "x": 0,
        "y": 32
      },
      "id": 14
    },
    {
      "datasource": "mysql",
      "description": "Shows which selected project(s) actually have BUG/INCIDENT-typed issues to compute a real escaped-defect rate from. A project marked \"Not classified\" doesn't have a zero defect rate \u2014 it means the underlying data isn't available to measure it yet for that project.",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true
      },
      "pluginVersion": "11.0.0",
      "targets": [
        {
          "datasource": "mysql",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT pm.project_name AS \"Project\",\n  SUM(i.type IN ('BUG','INCIDENT')) AS \"Classified Defects\",\n  COUNT(*) AS \"Total Issues\",\n  CASE WHEN SUM(i.type IN ('BUG','INCIDENT')) > 0 THEN 'Available' ELSE 'Not classified (Jira-only signal today)' END AS \"Defect-Rate Coverage\"\nFROM issues i\nJOIN board_issues bi ON bi.issue_id = i.id\nJOIN project_mapping pm ON pm.row_id = bi.board_id AND pm.`table` = 'boards'\nWHERE pm.project_name IN ($project)\nGROUP BY pm.project_name\nORDER BY 2 DESC",
          "refId": "A"
        }
      ],
      "title": "Defect-Rate Coverage by Project",
      "type": "table",
      "gridPos": {
        "h": 6,
        "w": 12,
        "x": 12,
        "y": 32
      },
      "id": 15
    }
  ],
  "refresh": "",
  "schemaVersion": 38,
  "tags": [
    "unified",
    "devlake",
    "ai",
    "throughput",
    "dashboard"
  ],
  "templating": {
    "list": [
      {
        "current": {
          "selected": false,
          "text": "All",
          "value": "$__all"
        },
        "datasource": "mysql",
        "definition": "SELECT DISTINCT project_name FROM project_pr_metrics ORDER BY 1",
        "hide": 0,
        "includeAll": true,
        "label": "Project",
        "multi": false,
        "name": "project",
        "options": [],
        "query": "SELECT DISTINCT project_name FROM project_pr_metrics ORDER BY 1",
        "refresh": 1,
        "regex": "",
        "skipUrlSync": false,
        "sort": 1,
        "type": "query"
      }
    ]
  },
  "time": {
    "from": "now-6M",
    "to": "now"
  },
  "timepicker": {},
  "timezone": "utc",
  "title": "AI Throughput Overview",
  "uid": "ai_throughput_overview",
  "version": 1,
  "weekStart": ""
}
